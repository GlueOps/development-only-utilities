# DEPRECATED. This file is kept only so that existing captain repositories that
# `source <(curl ...)` it at a pinned tools_version keep working. It must stay
# sourceable: never call `exit` here, only `return`.
deploy-glueops-platform() {
  echo "deploy-glueops-platform is deprecated: CRDs, ArgoCD and the platform are installed with captain_utils from your captain repo (menu: crds -> argocd -> crds -> glueops-platform); see the README of your captain repository" >&2
  return 1
}
